const search=document.getElementById('search');const segment=document.getElementById('segment');function filter(){const q=(search?.value||'').toLowerCase();const s=segment?.value||'';document.querySelectorAll('.op-card').forEach(card=>{const okText=card.dataset.title.includes(q);const okSegment=!s||card.dataset.segment===s;card.style.display=okText&&okSegment?'grid':'none';});}search?.addEventListener('input',filter);segment?.addEventListener('change',filter);async function analyze(id){const body=new URLSearchParams({id:String(id)});const response=await fetch('/api/analyze',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body});const data=await response.json();alert(data.analysis||'Não foi possível analisar esta oportunidade.');}